name: invent

on:
  push:

jobs:
  wolf:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        lamb: ["discourage", "wagon", "sensible", "equal", "vitamin"]
        gray: ["moon", "comprehensive", "abroad", "conscious"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar Qf.jar
